{% extends 'base.html.twig' %}
{% block body %}
    <p>test</p>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.price) }}
    <button type="submit">Create</button>
    {{ form_end(form) }}
{% endblock %}